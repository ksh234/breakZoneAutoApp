# breakZoneAutoApp 사용법

> 경고주(투자경고종목) 자동매매 시스템의 **사용 설명서 + 매수/매도 조건**.
> (기술 설계는 [ROADMAP.md](ROADMAP.md)·[docs/](docs/), 진행상황은 [PROGRESS.md](PROGRESS.md))
> 최종 갱신: 2026-09-03

---

## 1. 시스템 구성 (한눈에)

```
[Flutter 앱] ──제어/설정──▶ [Supabase] ◀──상태/명령──▶ [Python 봇] ──▶ [키움 모의계좌]
  폰/PC에서                 클라우드 중계 DB          집 PC에서          주문·체결·시세
  모니터링·제어              (실시간 동기화)          분석+전략+매매
```

- **봇(engine)**: 경고주 분석 → 조건 판단 → 키움 모의계좌에 주문. 집 PC에서 실행(나중에 클라우드 이관 가능).
- **앱(Flutter)**: 봇 상태를 실시간으로 보고, 시작/정지/설정을 제어. 폰이나 PC에서.
- **Supabase**: 둘 사이의 중계(DB + 실시간). 이미 클라우드에 있음.

> ⚠️ 현재 **모의투자(demo)** 단계입니다. 실제 돈은 오가지 않습니다.

---

## 2. 실행하기

### 봇 실행 (집 PC, 장중에 켜둠)
**가장 쉬운 방법:** 바탕화면의 **`breakZone 봇 시작.bat`** 더블클릭 (→ 저장소 `engine/run_bot.bat` 호출). 창이 열리고 로그가 흐르면 실행 중. 종료는 그 창에서 **Ctrl+C**(락 해제 후 안전 종료).
- 장 시간 밖에 켜도 됨(하트비트만 보내며 대기). 매매 시작/정지는 앱 제어 화면에서.
- 로그 파일: `engine/logs/bot.log`(5MB×10 회전). 창을 닫아도 파일에 남음.
- 켜자마자 "락 획득(호스트명)" 이 보이면 정상. "락 미획득 — 관찰 모드" 면 다른 봇(예: 클라우드)이 이미 운용 중.

수동 실행(터미널):
```powershell
cd D:\myWorkspace\breakZoneAutoApp\engine
.\.venv\Scripts\python.exe -m src.main
```

### 앱 실행 (PC 브라우저)
```powershell
cd D:\myWorkspace\breakZoneAutoApp\app
& "D:\dev\flutter\bin\flutter.bat" run -d chrome
```
- 실행 중 코드가 바뀌면 터미널에서 `R`(대문자) = 화면 새로고침(hot restart).

### 앱을 폰에 설치 (Android) — ✅ 배포 완료
```powershell
cd D:\myWorkspace\breakZoneAutoApp\app
& "D:\dev\flutter\bin\flutter.bat" build apk --release --split-per-abi
```
→ `app\build\app\outputs\flutter-apk\` 에 아키텍처별 APK 생성:
- **`app-arm64-v8a-release.apk`** (요즘 폰 대부분, ~18MB) ← 이거 쓰면 됨
- `app-armeabi-v7a-release.apk` (오래된 32비트 폰)

→ 이 APK를 폰으로 전송 → 열기 → **"알 수 없는 출처 허용"** → 설치 → 홈에 앱 아이콘.
→ 앱 실행 → **Supabase 계정으로 로그인**. 봇이 집 PC에서 돌면 폰에서 실시간 모니터링·제어.

> **빌드 환경 메모(이미 해결됨):** ① Android Studio(SDK) 필요. ② `android/gradle.properties`에 `kotlin.incremental=false`(D:프로젝트/C:펍캐시 드라이브 상이 회피). ③ `AndroidManifest.xml`에 INTERNET 권한. — 모두 반영돼 있어 `flutter build apk`만 돌리면 됨.
> `--split-per-abi` 없이 `flutter build apk --release` 하면 단일 `app-release.apk`(~51MB)가 나옴(전송 한도 넘으면 split 사용).

---

## 3. 앱 화면 (☰ 메뉴)

| 화면 | 내용 |
|---|---|
| **대시보드** | 봇 상태(running/stopped)·하트비트·평가금·현금·당일손익·보유수·주문수 |
| **후보** | 현재 경고주 목록(해제일 가까운 순). 해제일·D-day·해제가·현재가·하락비율·상태 |
| **포지션** | 보유 종목: 수량·평단·현재가·평가손익 |
| **주문** | 최근 주문(매수/매도·수량·가격·상태·사유) |
| **이벤트** | 봇 로그(매수/매도/차단/오류) 실시간 |
| **제어** | 시작/정지/일시정지/재개, 긴급정지(kill) |
| **설정** | 매매 파라미터 조절(§8) |

**후보 탭 상태 표기:** `정상`(모든 값 확정, 매수 가능) / `부분`·`미확정`(값 누락, 매수 안 함) / `오류`

---

## 4. 매수 조건 (자동 매수)

자동매매가 **켜져 있을 때(enabled ON)**, 아래 **모든 조건**을 만족하면 매수합니다.

### 신규 진입 (미보유 종목)
1. **매수 유효구간** — 해제일이 지나지 않음 (오늘 ≤ 해제일). *해제 전에 사서 해제 후 급등에 파는 전략.*
2. **상태 = 정상(ok)** — 해제금액 계산에 필요한 값(T-5·T-15·최고가·현재가)이 **모두 확정**. 하나라도 없으면 매수 안 함.
3. **하락비율 ≥ 진입 기준(기본 30%)** — 현재가가 해제금액보다 30% 이상 낮음(매수구간).
4. **저가 반등 (선택)** — `저가 반등 매수`(기본 0)를 켜면(예 3%), 매수구간 **저점 대비 그만큼 반등**해야 매수. *계속 급락 중일 때 매수 방지.* 0이면 즉시.
5. **현재가 < Envelope 하단** — 이동평균 밴드 하단 아래(과매도).
6. **현재가 ≥ 최소 매수가(기본 1,000원)** — 저가주 제외.
7. **보유 종목 수 < 최대 보유종목수(기본 5)**
8. **리스크 가드 통과** (§6)

→ **분할매수:** 1회에 `종목당 총액 × 1회 비중`(기본 100만 × 30% = 30만)씩. 누적 매수액이 종목당 총액을 넘지 않게(약 3~4회).

### 추가매수 / 물타기 (보유 종목)
- 위 공통 조건(유효구간·상태 정상·최소가·예산)을 만족하고,
- **현재가 ≤ 평단가 × (1 − 추가매수 기준, 기본 7%)** 이면 1회분 추가 매수.

---

## 5. 매도 조건 (자동 매도)

봇이 **running이면 항상**(enabled와 무관하게) 보유 종목을 아래 규칙으로 매도합니다. *해제일이 지나 후보에서 빠진 보유 종목도 계속 관리됩니다.*

1. **분할 익절 시작** — `현재가 > Envelope 상단` **그리고** `수익률 ≥ 익절 기준(기본 +15%)` → **보유의 50%(기본) 매도.** 이후 고점 추적 시작.
2. **트레일링 전량** — 분할매도 후, **고점 대비 하락 기준(기본 −5%)** 만큼 떨어지면 → **잔량 전량 매도.**
3. **2차 상승 전량** — 분할매도 후, **1차 매도가 대비 2차 상승 기준(기본 0=끔)** 이상 오르면 → **잔량 전량 매도.** *(평단 아니라 1차 매도한 가격 기준. 예: 12,000원에 1차 매도, 기준 10% → 13,200원에 잔량 매도)*
4. **급등 전량** — 분할매도 후, 전일 종가 대비 **급등 기준(기본 +29%)** 이상 오르면 → **잔량 전량 매도.**
5. **긴급정지(kill)** — 앱/서버에서 kill → **전량 시장가 청산 + 정지.**

> 분할익절(1번)은 종목당 **한 번만** 일어납니다. 그 뒤 잔량은 2~4번 규칙으로만 팔립니다(+15% 조건은 다시 보지 않음). 분할매도 후 우선순위: 급등(4) → 2차 상승(3) → 트레일링(2).

---

## 6. 안전장치

**리스크 가드** (주문 직전 검사, 하나라도 실패 시 차단):
- 종목당 한도(누적 매수 ≤ 종목당 총액) · 보유수 상한 · 현금 충분 · 수량 검증
- **평가손실 한도** — 보유 전체 **평가손실**이 한도(기본 −50만) 도달 시 **신규매수 중단**(청산은 계속). *하락장 물타기 폭증 방지.*
- 가격 sanity(±30% 범위) · 중복주문 방지

**kill-switch** — 앱 제어화면의 긴급정지(2단계 확인) → 전량청산 + 정지.

**이중 실행 방지(락)** — 봇이 둘(집 PC + 클라우드) 떠도 **락을 잡은 하나만** 주문·기록. 나머지는 관찰 모드로 대기하다가 보유 봇이 죽으면(하트비트 90초 끊김) 자동 승계. 이벤트 탭에 "봇 LIVE" / "락 상실" 로 표시.

**드라이런** — PC `.env` 에 `BOT_DRY_RUN=1` 이면 주문도 Supabase 기록도 전혀 없이 로그에 `[DRY] 매수 시뮬 …` 판정만. 새 코드 관찰용. 클라우드 이관 후 PC 는 이 값으로 두는 것을 권장.

**재시작 복원** — 분할매수 횟수·분할매도 후 고점·매수구간 저점은 DB(`strategy_state`)에 저장되어 봇을 재시작/재배포해도 유지됨.

---

## 7. 제어 방법

| 조작 | 매수 | 매도(청산) | 설명 |
|---|---|---|---|
| **정지(stop)** | ❌ | ❌ | 봇 멈춤(하트비트만) |
| **자동매매 OFF** (running) | ❌ | ✅ | 신규 매수 중단, 보유 청산은 계속 |
| **자동매매 ON** (running) | ✅ | ✅ | 완전 자동 |
| **긴급정지(kill)** | 전량청산 + 정지 |

- **일시정지/재개(pause/resume)**: 잠깐 멈췄다 재개.
- **처음엔** 자동매매 OFF(관망)로 두고 하트비트·후보를 확인한 뒤, 익숙해지면 ON.

---

## 8. 설정 파라미터 (앱 설정 화면)

모든 값은 앱에서 조절 가능. **저장 → 봇에 즉시 반영**(앱이 `set_param` 명령 전송) + 봇이 **30초마다 settings 재로드**(명령 유실·대시보드 직접 수정 대비). 반영되면 이벤트 탭에 **"설정 반영"** 이 뜸(값이 실제로 바뀐 경우만).

### ⚙️ 기본 설정
| 파라미터 | 의미 | 기본값 |
|---|---|---|
| 최소 매수가 | 이 미만 종목은 매수 안 함(0=무제한) | 1,000원 |
| 최대 보유종목수 | 동시 보유 상한 | 5 |
| 평가손실 매수중단 한도 | 보유 평가손실이 이만큼이면 신규매수 중단 | 500,000원 |
| 평가 주기 | 매매 판단 주기(초) | 5 |

### ⬇️ 매수 설정
| 파라미터 | 의미 | 기본값 |
|---|---|---|
| 진입 하락비율 기준(%) | 해제가 대비 이 % 이상 하락해야 매수구간 | 30 |
| 저가 반등 매수(0~1) | 저점서 이만큼 오르면 매수(0=즉시) | 0.0 |
| 종목당 총 투자액 | 한 종목에 넣을 총액 | 1,000,000원 |
| 1회 매수 비중(0~1) | 총액 대비 1회 매수(0.3=30%) | 0.30 |
| 최대 분할매수 횟수 | 분할매수 최대 횟수 | 4 |
| 추가매수 하락 기준(0~1) | 평단 대비 이만큼 하락 시 물타기(0.07=7%) | 0.07 |

### ⬆️ 매도 설정
| 파라미터 | 의미 | 기본값 |
|---|---|---|
| 분할익절 수익률(%) | 이 수익률+엔벨상단이면 분할매도 시작 | 15 |
| 첫 분할매도 비중(0~1) | 익절 시 매도 비율(0.5=50%) | 0.50 |
| 분할매도후 하락 전량(0~1) | 분할매도 후 고점 대비 이만큼 하락 시 전량 | 0.05 |
| 2차 상승 전량매도(%) | 분할매도 후 **1차 매도가** 대비 이 % 이상 오르면 잔량 전량. 0=끔 | 0 |
| 급등 전량매도 기준(%) | 분할매도 후 전일 대비 이 % 이상 상승 시 전량 | 29 |

### 📈 Envelope 지표
| 파라미터 | 의미 | 기본값 |
|---|---|---|
| Envelope 기간(일) | 이동평균 기간 | 20 |
| Envelope 밴드(0~1) | 상·하단 폭(0.1=±10%) | 0.10 |

---

## 9. 자주 하는 시나리오

**A. 관망(모니터링만)** — 봇 실행 → 앱 제어에서 **시작** → 설정 **자동매매 OFF**. 매매 없이 상태·후보만 관찰.

**B. 자동매매 시작** — 봇 실행 → **시작** → 설정에서 파라미터 확인 → **자동매매 ON**. 조건 맞으면 모의 매수/매도.

**C. 급하게 멈추기** — 제어 → **긴급정지(kill)**: 전량청산 + 정지. 또는 **정지**: 매매만 멈춤(보유 유지).

---

## 10. 문제 해결(FAQ)

- **후보 현재가/하락비율이 0?** → 봇을 재시작하면 채워집니다(봇이 키움 현재가로 갱신).
- **매수가 안 일어남?** → 진입 조건이 까다로워 조건 맞는 종목이 없을 수 있음(정상). 후보 탭에서 **상태 정상 + 하락비율 30%↑(주황)** 종목이 있는지 확인.
- **해제일 지난(D+) 종목이 후보에 있음?** → 아직 KRX 경고 중이면 정상. 단 **신규매수는 안 함**(매수 적기는 해제 전).
- **봇 재시작하면 저점 추적 리셋?** → 네, 메모리 기반이라 재시작 시 리셋 후 재형성. 장중 계속 켜두면 됩니다.
- **로그 확인** → 봇 터미널에 매 판단이 찍힘. 매수/매도/차단은 앱 **이벤트** 탭에도.

---

## 11. 매매법 변경 이력 (코드 기준, 최신 위)

| 날짜 | 변경 | 종류 | 코드/설정 |
|---|---|---|---|
| 2026-09-04 | **2차 상승 전량매도** — 분할매도 후 **1차 매도가** 대비 +N% 이상이면 잔량 전량. 기본 0=끔. 우선순위 상한가 → 2차 상승 → 트레일링 | 매도 추가 | `post_sell_gain_pct`(%) |
| 2026-09-03 | **저가 반등 매수** — 매수구간 저점 대비 +N% 반등해야 매수(급락 중 매수 방지). 저점은 구간 이탈 시 리셋. 0=즉시 | 매수 조건 추가 | `entry_rebound_pct`(0~1) |
| 2026-09-03 | **해제일 지난 종목 신규·추가매수 제외**(매수 적기 T-5~해제일). 보유분 청산은 계속 | 매수 조건 추가 | 코드 고정 |
| 2026-09-03 | **값 확정 게이트** — 후보 상태가 정상(ok)일 때만 매수(T-5·T-15·최고가·현재가 전부 있어야). partial/pending 차단 | 매수 조건 추가 | 코드 고정 |
| 2026-09-03 | 자동매매 ON/OFF 의미 명확화 — **OFF 는 매수만 중단**, 청산은 running 이면 항상 | 의미 정리 | `enabled` |
| 2026-09-02 | 손실 한도를 실현손실 → **보유 전체 평가손실** 기준으로. 한도 도달 시 신규매수 중단(청산 계속) | 리스크 변경 | `max_unrealized_loss_krw` |
| 2026-09-02 | 진입 하락비율 범위(30~40%) → **단일 기준(≥30%, 상한 없음)** | 매수 조건 변경 | `entry_drop_pct` |
| 2026-09-02 | 급등 전량매도 기준(상한가 근사 +29%)을 하드코딩 → **설정값**으로 | 설정화 | `limit_up_pct` |
| 2026-09-02 | **최소 매수가** 필터(기본 1,000원 미만 매수 안 함, 신규·추가 공통, 0=무제한) | 매수 조건 추가 | `min_price` |
| 2026-09-02 | **기본 전략 확정(D-013)** — 매수: 하락비율 ≥ 기준 AND 현재가 < Envelope(20일 ±10%) 하단 → 분할매수(총액×30%씩, 총액 상한). 물타기: 평단 −7%. 매도: 현재가 > Envelope 상단 AND +15% → 50% 익절 → 이후 고점 −5% 전량 / 상한가 전량. 주문 지정가 | 최초 | — |

> 실제 판정 코드는 `engine/src/strategy/rules.py`(진입 `should_enter` / 청산 `should_exit`), 리스크 가드는 `risk.py`. 명세는 `docs/03-strategy-spec.md` §2~3.
