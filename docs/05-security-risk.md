# 05 · 보안 · 리스크 · 실계좌 전환

> 실제 자금이 오가는 시스템. 이 문서의 항목은 "있으면 좋은 것"이 아니라 **전제조건**이다.

---

## 1. 시크릿 관리

| 시크릿 | 위치 | 규칙 |
|---|---|---|
| 키움 App Key/Secret (demo/real) | 개발: engine `.env` / 실전: **OS 키체인**(Windows 자격증명 관리자) 또는 클라우드 시크릿매니저 | 깃 커밋 금지, 로그 출력 금지 |
| Supabase `service_role` | engine `.env`(서버 전용) | **앱에 절대 미포함** |
| Supabase `anon` | 앱 | 공개 가능(RLS로 보호) |
| Firebase 서비스계정 (선택 · Phase 5-B 푸시 채택 시) | Edge Function secret | 깃 금지 |

- `.gitignore`: `.env`, `.env.*`, `*.local`, `google-services*.json`, `*serviceAccount*.json`, 키체인 export 등.
- 토큰/키를 콘솔·이벤트·DB에 **평문 기록 금지**. 마스킹 유틸 사용.
- 실전 키는 개발 PC와 분리(가능하면 실전은 클라우드 시크릿매니저에서만).

## 2. 리스크 가드 (봇 강제 — docs/03 §3 재확인)

- 종목당 한도 / 보유수 상한 / 현금 검증 / 평가손실 한도 / 가격 sanity / 중복주문 가드 / 수량 검증. (구현: `strategy/risk.py::ok_buy·ok_sell`)
- **mode='real' 강제 한도:** 실전에서는 코드가 아래를 **하드 상한**으로 강제(설정이 더 커도 클램프). 현재 `risk.clamp_real_mode` 는 골격만 — Phase 7에서 값 확정:
  - 초기 `per_stock_krw` 소액(예: 카나리 100,000~300,000).
  - `max_positions` 축소.
  - `max_unrealized_loss_krw` 보수적.
  - 규칙 위반 시도 시 주문 차단 + `critical` 이벤트.

## 3. Kill-switch (다층)

1. **앱 버튼**(2단계 확인) → `kill` 명령.
2. **서버 자동:** 치명 오류/일손실 초과/브로커 반복거부 → 자동 kill.
3. **동작:** 신규진입 즉시 중단 → 보유 **전량 시장가 청산** 시도 → `stopped` → `critical` 이벤트(인앱 알림, 채택 시 푸시).
4. 청산 실패분은 재시도 + 앱에 명확히 노출(수동 개입 유도).

## 4. 이중 실행 방지 (집 PC ↔ 클라우드)

같은 키움 계좌에 봇이 둘이면 **이중 주문** 위험. 두 겹으로 방지(2026-09-03 구현, 마이그레이션 0005):

**① 분산 락 `bot_lock`** (id=1 단일행, `holder_id`, `heartbeat_at`)
- DB 함수 `acquire_bot_lock(owner, holder, stale_sec)` 하나로 **획득=갱신**: 내가 보유 중이면 heartbeat 갱신, 비었거나 heartbeat 가 `stale_sec`(기본 90초) 이상 오래됐으면 승계. `INSERT … ON CONFLICT DO UPDATE … WHERE` 로 원자적. `release_bot_lock` 으로 정상 종료 시 해제. 앱 사용자(anon/authenticated)는 실행 권한 없음(봇 secret key 만).
- 봇(`main.LockKeeper`): 시작 시 획득 시도 → 성공하면 **LIVE**(싱글턴 보장·상태 복원·명령 리스너). 실패하면 **관찰 모드**로 대기하며 `BOT_LOCK_RENEW_SEC`(15초)마다 재시도 → 보유자가 죽어 stale 되면 자동 승계(이벤트 "봇 LIVE"). 보유 중 갱신 실패 시 즉시 관찰 모드 + critical 이벤트 "락 상실".
- 우선순위 없음 = **먼저 잡은 쪽 유지**. PC 에서 주문 포함 테스트가 필요하면 클라우드 서비스를 잠깐 내려 락을 풀고 진행.

**② 드라이런 `BOT_DRY_RUN=1`** (PC 기본 권장, 클라우드 이관 후)
- 락과 무관하게 항상 관찰 모드. `StrategyEngine.set_live(False)` → 주문 함수는 `[DRY] 매수/매도 시뮬 …` 로그만(종목·사유당 하루 1회), `relay` 는 `DryRunRelay` 로 교체되어 **Supabase 쓰기 전부 무시**(하트비트·후보·포지션·주문·이벤트·전략상태·명령 ack). 읽기(settings·strategy_state)는 실제 값. 명령 리스너도 안 켬(클라우드 봇 명령을 가로채지 않음).
- 용도: 새 코드를 장중 PC 에서 실시간 시세로 돌려 판정만 관찰. 클라우드 봇 상태를 덮어쓰지 않음.

관찰 모드(①실패)와 드라이런(②)은 엔진 관점에서 동일(`live=False`).

## 5. 장애 시나리오 → 대응 (모두 `events` 인앱 알림, 푸시 채택 시 푸시도)

| 시나리오 | 감지 | 대응 |
|---|---|---|
| 봇 다운/크래시 | 하트비트 정지 | 앱 "연결 끊김" + (클라우드)자동 재시작. 미체결 주문 상태 점검 |
| 네트워크 단절 | 요청 실패/재연결 | 오프라인 버퍼링 후 flush, 지속 시 신규진입 보류 |
| 토큰 만료/인증 실패 | 401 | 재발급 재시도, 실패 지속 시 정지+알림 |
| 주문 거부(reject) | 응답코드 | 사유 로깅+이벤트, 반복 시 해당 종목 차단 |
| 부분체결 | 체결조회 | 잔량 추적, 포지션/주문 상태 정합 유지 |
| Rate limit(429) | 응답 | 백오프+큐, 임계 초과 시 주기 완화 |
| 시세 이상치 | sanity | 해당 틱 무시, 주문 sanity가 2차 방어 |
| 이중 실행 | 락 상실 | 매매 중단, 알림 |

## 6. 데이터 정합성

- 주문 상태기계는 봇이 소유. Supabase는 미러. 재시작 시 브로커 잔고/미체결을 **재동기화**(source of truth = 브로커).
- 봇 재시작 시 브로커에서 positions/미체결을 다시 읽어 Supabase를 재구성(로컬 상태 맹신 금지).

## 7. 실계좌 전환 체크리스트 (Phase 7 게이트 — 전부 통과해야 real)

- [ ] Phase 6 모의 전진검증 통과(치명 버그 0, 기간 충족).
- [ ] 백테스트/모의로 전략 파라미터 확정, docs/03 반영.
- [ ] kill-switch 앱·서버 양쪽 실증.
- [ ] mode='real' 하드 상한 동작 확인(설정 초과 클램프 테스트).
- [ ] 시크릿을 실전용으로 분리·안전 보관.
- [ ] 이중 실행 방지 락 동작.
- [ ] 모든 장애 시나리오에 알림/자동정지 매핑·테스트.
- [ ] 롤백 절차 문서화(문제 시 즉시 stop+청산+원인분석).
- [ ] **소액 카나리**로 시작(며칠 관찰) → 단계적 한도 상향.
- [ ] 세금/수수료/증권거래세 반영한 손익 인식(리스크 한도 계산에 포함).

## 8. 법·운영 유의(비법률 자문)

- 개인용/본인 계좌 자동매매 전제. 증권사 API 이용약관(자동매매 허용 범위·호출한도) 준수.
- 시스템 오작동 손실은 사용자 책임 — 그래서 모의검증·한도·kill-switch를 강제한다.
- 이 문서는 법률/투자 자문이 아니다.
